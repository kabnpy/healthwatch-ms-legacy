import { createFileRoute, Outlet } from "@tanstack/react-router"

export const Route = createFileRoute("/_layout/catalog")({
  component: () => (
    <div className="border-8 border-purple-500 p-4">
      <h1 className="text-2xl font-bold">ROOT CATALOG LAYOUT</h1>
      <Outlet />
    </div>
  ),
})
