{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "bash scripts/prestart.sh && fastapi run --workers 4 app/main.py",
    "healthcheckPath": "/api/v1/utils/health-check/",
    "healthcheckTimeout": 100
  }
}
